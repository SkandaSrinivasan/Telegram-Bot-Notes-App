{
  "name": "notes-server",
  
  "version": "1.0.0",
  "description": "Api server for notes in mongo",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-ui": "rm -rf build && cd ../notes-app/ && npm run build --prod && cp -r build ../Notes-server",
    "dev": "nodemon index.js",
    "deploy:full": "npm run build-ui && git add . && git commit -m uibuild && git push heroku master:main"
  },
  "author": "skanda",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^3.6.10",
    "mongoose": "^5.13.2"
  },
  "devDependencies": {
    "eslint": "^7.30.0",
    "nodemon": "^2.0.12"
  }
}
